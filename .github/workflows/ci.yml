name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Configurar Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 20

    - name: Instalar dependências do backend
      run: |
        cd backend
        if [ -f package.json ]; then
          npm install
        else
          echo "Sem backend implementado"
        fi

    - name: Instalar dependências do frontend
      run: |
        cd frontend
        if [ -f package.json ]; then
          npm install
          npm run build --if-present
        else
          echo "Sem frontend implementado"
        fi

    - name: Deploy para Vercel
      run: |
        npm install -g vercel
        vercel --prod --token=${{ secrets.VERCEL_TOKEN }} --yes

# O RENDER NÃO ESTÁ SENDO MAIS USADO, POIS O BACKEND FOI MIGRADO PARA AZURE

 #   - name: Deploy do Backend no Render
 #     env:
 #       RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
 #       RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
 #     run: |
 #       echo "Iniciando deploy no Render..."
 #       curl -X POST "https://api.render.com/v1/services/${RENDER_SERVICE_ID}/deploys" \
 #         -H "Accept: application/json" \
 #         -H "Authorization: Bearer ${RENDER_API_KEY}" \
 #         -d ''
#      echo "Deploy do backend disparado com sucesso!"